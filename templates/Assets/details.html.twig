{% extends 'base.html.twig' %}

{% block title %}Detaily majetku{% endblock %}

{% block body %}
    <div class="flex h-screen bg-gray-50 dark:bg-gray-900" :class="{ 'overflow-hidden': isSideMenuOpen }">
        <!-- Desktop sidebar -->
        <aside class="z-20 hidden w-64 overflow-y-auto bg-white dark:bg-gray-800 md:block flex-shrink-0">
            <div class="py-4 text-gray-500 dark:text-gray-400">
                <a class="ml-6 text-lg font-bold text-gray-800 dark:text-gray-200" href="/">
                    Evidence majetku
                </a>

                <div class="px-6 my-6">
                    <a href="/create">
                        <button class="flex items-center justify-between w-full px-4 py-2 text-sm font-medium leading-5 text-white transition-colors duration-150 bg-purple-600 border border-transparent rounded-lg active:bg-purple-600 hover:bg-purple-700 focus:outline-none focus:shadow-outline-purple">
                            Přidat nový majetek
                            <span class="ml-2" aria-hidden="true">+</span>
                        </button>
                    </a>
                </div>

                <ul class="mt-6">
                    <li class="relative px-6 py-3">
                        <a class="inline-flex items-center w-full text-sm font-semibold transition-colors duration-150 hover:text-gray-800 dark:hover:text-gray-200" href="/">
                            <svg class="w-5 h-5" aria-hidden="true" fill="none" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" stroke="currentColor">
                                <path d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
                            </svg>
                            <span class="ml-4">Přehled</span>
                        </a>
                    </li>
                </ul>

                <ul>
                    <li class="relative px-6 py-3">
                        <a class="inline-flex items-center w-full text-sm font-semibold transition-colors duration-150 hover:text-gray-800 dark:hover:text-gray-200" href="/list">
                            <svg class="w-5 h-5" aria-hidden="true" fill="none" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" stroke="currentColor">
                                <path d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01"></path>
                            </svg>
                            <span class="ml-4">Seznam majetku</span>
                        </a>
                    </li>

                    <li class="relative px-6 py-3">
                        <a class="inline-flex items-center w-full text-sm font-semibold transition-colors duration-150 hover:text-gray-800 dark:hover:text-gray-200" href="/users">
                            <svg class="w-5 h-5" aria-hidden="true" fill="none" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" stroke="currentColor">
                                <path d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
                            </svg>
                            <span class="ml-4">Uživatelé</span>
                        </a>
                    </li>

                    <li class="relative px-6 py-3">
                        <a class="inline-flex items-center w-full text-sm font-semibold transition-colors duration-150 hover:text-gray-800 dark:hover:text-gray-200" href="/types">
                            <svg class="w-5 h-5" aria-hidden="true" fill="none" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" stroke="currentColor">
                                <path d="M4 6h16M4 10h16M4 14h16M4 18h16"></path>
                            </svg>
                            <span class="ml-4">Druhy</span>
                        </a>
                    </li>

                    <li class="relative px-6 py-3">
                        <a class="inline-flex items-center w-full text-sm font-semibold transition-colors duration-150 hover:text-gray-800 dark:hover:text-gray-200" href="/categories">
                            <svg class="w-5 h-5" aria-hidden="true" fill="none" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" stroke="currentColor">
                                <path d="M4 6h16M4 10h16M4 14h16M4 18h16"></path>
                            </svg>
                            <span class="ml-4">Kategorie</span>
                        </a>
                    </li>

                    <li class="relative px-6 py-3">
                        <a class="inline-flex items-center w-full text-sm font-semibold transition-colors duration-150 hover:text-gray-800 dark:hover:text-gray-200" href="/locations">
                            <svg class="w-5 h-5" aria-hidden="true" fill="none" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" stroke="currentColor">
                                <path d="M4 6h16M4 10h16M4 14h16M4 18h16"></path>
                            </svg>
                            <span class="ml-4">Umístění</span>
                        </a>
                    </li>

                    <li class="relative px-6 py-3">
                        <a class="inline-flex items-center w-full text-sm font-semibold transition-colors duration-150 hover:text-gray-800 dark:hover:text-gray-200" href="/workplaces">
                            <svg class="w-5 h-5" aria-hidden="true" fill="none" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" stroke="currentColor">
                                <path d="M4 6h16M4 10h16M4 14h16M4 18h16"></path>
                            </svg>
                            <span class="ml-4">Pracoviště</span>
                        </a>
                    </li>

                    <li>
                        <hr class="my-2 border-purple-400" />
                    </li>

                    <li>
                        <div class="px-6 my-6">
                            <a href="/createProperty">
                                <button class="flex items-center justify-between w-full px-4 py-2 text-sm font-medium leading-5 text-white transition-colors duration-150 border border-gray-300 rounded-lg active:bg-purple-600 hover:bg-gray-100 hover:text-gray-700 focus:outline-none focus:shadow-outline-purple">
                                    Přidat hodnoty
                                    <span class="ml-2" aria-hidden="true">+</span>
                                </button>
                            </a>
                        </div>
                    </li>
                </ul>
            </div>
        </aside>

        <div class="flex flex-col flex-1 w-full">
            <header class="z-10 py-4 bg-white shadow-md dark:bg-gray-800">
                <div class="container flex items-center justify-between h-full px-6 mx-auto text-purple-600 dark:text-purple-300">
                    <div class="flex justify-center flex-1 lg:mr-32">
                    </div>

                    <ul class="flex items-center flex-shrink-0 space-x-6">
                        <!-- Profile menu -->
                        <li class="relative">
                            {% if app.user ? app.user.profilePic %}
                                <div class="relative hidden w-8 h-8 mr-3 rounded-full md:block">
                                    <img class="object-cover w-full h-full rounded-full" src="{{ app.user.profilePic }}" alt="" loading="lazy">
                                    <div class="absolute inset-0 rounded-full shadow-inner" aria-hidden="true"></div>
                                </div>
                            {% else %}
                            {% endif %}
                        </li>
                        <li class="relative">
                            <p class="align-middle rounded-full focus:shadow-outline-purple focus:outline-none">
                                {{ app.user ? app.user.email  }}
                            </p>
                        </li>

                        <li class="relative">
                            <a class="align-middle rounded-full focus:shadow-outline-purple focus:outline-none"
                               href="/{{ app.user ? 'logout' : 'login' }}"
                               @click="isOpen = false">
                                {{ app.user ? 'Logout' : 'Login' }}
                            </a>
                        </li>
                    </ul>
                </div>
            </header>

            <main class="h-full overflow-y-auto">
                <div class="container px-6 mx-auto grid">
                    <br><br>
                    <main class="h-full overflow-y-auto">
                        <div class="container px-6 mx-auto grid">

                            <ul>
                                <li class="inline-block">
                                    <a href="{{ path('list') }}" class="text-m text-gray-600 dark:text-gray-400 hover:underline">Seznam majetku</a>
                                </li>
                                <li class="inline-block text-m text-gray-600 dark:text-gray-400 hover:underline">
                                    >
                                </li>
                                <li class="inline-block">
                                    <b>
                                    <a href="/details/{{ assets.id }}" class="text-m text-purple-600 dark:text-purple-400 underline">Detaily {{ assets.name }}</a>
                                    </b>
                                </li>
                            </ul>

                            <h2 class="my-6 text-2xl font-semibold text-gray-700 dark:text-gray-200">
                                Detaily majetku {{ assets.name }}
                                <hr class="my-2 border-purple-400" />

                                <span class="flex col-span-4 mt-2 sm:mt-auto sm:justify-end">
                                    <div class="inline-flex items-center">
                                        <a href="/edit/{{ assets.id }}">
                                            <button class="mr-2 items-center justify-between px-2 py-2 text-sm font-medium leading-5 text-white transition-colors duration-150 bg-purple-600 border border-transparent rounded-lg active:bg-purple-600 hover:bg-purple-700 focus:outline-none focus:shadow-outline-purple" aria-label="Edit">
                                                <svg class="w-5 h-5" aria-hidden="true" fill="currentColor" viewBox="0 0 20 20">
                                                    <path d="M13.586 3.586a2 2 0 112.828 2.828l-.793.793-2.828-2.828.793-.793zM11.379 5.793L3 14.172V17h2.828l8.38-8.379-2.83-2.828z"></path>
                                                </svg>
                                            </button>
                                        </a>

                                        <a href="#" onclick="openCustomConfirmationModal('{{ assets.name|upper }}')">
                                            <button class="items-center justify-between px-2 py-2 text-sm font-medium leading-5 text-white transition-colors duration-150 bg-purple-600 border border-transparent rounded-lg active:bg-purple-600 hover:bg-purple-700 focus:outline-none focus:shadow-outline-purple" aria-label="Delete">
                                                <svg class="w-5 h-5" aria-hidden="true" fill="none" viewBox="0 0 20 20" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" stroke="currentColor">
                                                    <path d="M13.477 14.89A6 6 0 015.11 6.524l8.367 8.368zm1.414-1.414L6.524 5.11a6 6 0 018.367 8.367zM18 10a8 8 0 11-16 0 8 8 0 0116 0z"
                                                    ></path>
                                                </svg>
                                            </button>
                                        </a>
                                        <div class="min-w-0 p-4 bg-white rounded-lg shadow-xs dark:bg-gray-800 border border-purple-400 text-xl" id="customConfirmationModal" style="display: none; position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%); ">
                                            <h5 class="text-red-700">Upozornění!</h5>
                                            <p>Opravdu chcete majetek {{ assets.name|upper }} smazat?</p>
                                            <br>
                                            <a href="/delete/{{ assets.id }}" class="items-center justify-between px-2 py-2 text-sm font-medium leading-5 text-white transition-colors duration-150 bg-purple-600 border border-transparent rounded-lg active:bg-purple-600 hover:bg-purple-700 focus:outline-none focus:shadow-outline-purple" aria-label="Delete">Ano, smazat</a>
                                            <button onclick="closeCustomConfirmationModal()" class="items-center justify-between px-2 py-2 text-sm font-medium leading-5 text-white transition-colors duration-150 bg-purple-600 border border-transparent rounded-lg active:bg-purple-600 hover:bg-purple-700 focus:outline-none focus:shadow-outline-purple" aria-label="Delete">Zrušit</button>
                                        </div>
                                    </div>
                                </span>
                            </h2>

                            <div class="px-4 py-3 mb-8 bg-white rounded-lg shadow-md dark:bg-gray-800 border border-purple-400">
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Název:</b> {{ assets.name }}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Inventární číslo:</b> {{ assets.inventoryNumber }}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Cena:</b> {{ assets.unitPrice }} CZK
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Záruka v letech:</b> {{ assets.guaranteePeriod }}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Typ:</b> {% if assets.typeAsset %} <a href="/typeDetails/{{ assets.typeAsset.id }}"> {{ assets.typeAsset }} </a> {% endif %}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Umístění:</b> {% if assets.locationAsset %} <a href="/locationDetails/{{ assets.locationAsset.id }}"> {{ assets.locationAsset }} </a>{% endif %}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Přiřazeno:</b> {% if assets.ownedBy %} <a href="/userDetails/{{ assets.ownedBy.id }}"> {{ assets.ownedBy }} </a>{% endif %}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Popis:</b> {{ assets.description }}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Status vyřazení:</b> {{ assets.eliminated }}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Kategorie:</b> {% if assets.categoryAsset %} <a href="/categoryDetails/{{ assets.categoryAsset.id }}"> Redbit-{{ category }} </a>{% endif %}
                                </p>
{#                                <p class="text-m text-gray-600 dark:text-gray-400">#}
{#                                    <b class="text-red-700">OLDKategorie:</b> Redbit-{{ assets.categoryAsset }}#}
{#                                </p>#}
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Pracoviště:</b> {% if assets.workplaceAsset %} <a href="/workplaceDetails/{{ assets.workplaceAsset.id }}"> {{ assets.workplaceAsset }} </a>{% endif %}
                                </p>
{#                                <img src="{{ qrCode.dataUri }}" alt="QR Code">#}
{#                                <img src="{{ qrCode.data }}" alt="QR Code">#}
{#                                <p class="text-m text-gray-600 dark:text-gray-400">#}
{#                                    <b class="text-purple-400">QR Code:</b> <img src="{{ qrCode }}" alt="QR Code">#}
{#                                </p>#}
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">QR Code:</b>
                                <form id="generate-form">
                                    <input
                                            id="url"
                                            type="hidden"
                                            placeholder="Enter a URL"
                                            class="w-full border-2 border-gray-200 rounded p-3 text-grey-dark mr-2 focus:outline-none mb-5"
                                            required
                                            value="http://127.0.0.1:8000/details/{{ assets.id }}"
                                    />
                                    <button
                                            class="px-3 py-3 font-medium leading-5 text-white transition-colors duration-150 bg-purple-600 border border-transparent rounded-lg active:bg-purple-600 hover:bg-purple-700 focus:outline-none focus:shadow-outline-purple"
                                            type="submit"
                                    >
                                        Vygenerovat QR Code
                                    </button>
                                </form>
                                <div id="spinner" role="status"></div>
                                <div id="qrcode" class="m-auto"></div>
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400" id="body">
                                    <b class="text-purple-400">Karta:</b>
                                    <button onclick="GFG_Fun()">
                                        click here
                                    </button>
                                <h2 id="result" style="color:green;"></h2>
{#                                    <img src="{{ image }}" alt="Asset Image">#}
                                </p>
                            </div>

                            <div class="px-4 py-3 mb-8 bg-white rounded-lg shadow-md dark:bg-gray-800 border border-purple-400">
{#                                <p class="text-m text-gray-600 dark:text-gray-400">#}
{#                                    <b class="text-purple-400">Vlastník:</b> {{ assets.owner }}#}
{#                                </p>#}
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Dodavatel:</b> {{ assets.supplier }}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Výrobce:</b> {{ assets.manufacturer }}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Výrobní číslo:</b> {{ assets.manufacturingNumber }}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Číslo objednávky:</b> {{ assets.orderNumber }}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">URL objednávky:</b> <a href="{{ assets.orderURL }}" class="hover:underline"> {{ assets.orderURL }} </a>
                                </p>
                            </div>

                            <div class="px-4 py-3 mb-8 bg-white rounded-lg shadow-md dark:bg-gray-800 border border-purple-400">
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Datum vytvoření:</b>{%  if assets.dateCreated %} {{ assets.dateCreated | date('d.m.Y') }} {% endif %}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400" id="demo">
                                    <b class="text-purple-400">Datum zařazení:</b>{% if assets.subsumptionDate %}{{ assets.subsumptionDate | date('d.m.Y') }} {% endif %}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Datum vyřazení:</b>{% if assets.eliminationDate %} {{ assets.eliminationDate | date('d.m.Y') }} {% endif %}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Datum zakoupení:</b>{% if assets.dateBought %} {{ assets.dateBought | date('d.m.Y') }} {% endif %}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Datum obdržení:</b>{% if assets.dateReceived %} {{ assets.dateReceived | date('d.m.Y') }} {% endif %}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Příští servis:</b>{% if assets.nextServiceDue %} {{ assets.nextServiceDue | date('d.m.Y') }} {% endif %}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Servisní interval:</b> {{ assets.serviceInterval }}
                                </p>
                            </div>

                            <div class="px-4 py-3 mb-8 bg-white rounded-lg shadow-md dark:bg-gray-800 border border-purple-400">
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Poznámka:</b> {{ assets.note }}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Reklamace:</b> {{ assets.complaint }}
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Nahrané dokumenty:</b>
                                    <ul>
                                    {% for file in assets.files %}
                                        <li class="text-m text-gray-600 dark:text-gray-400">
                                    <a href="{{ path('download_file', {'documentPath': file.fileName}) }}" class="hover:underline">{{ file.fileName }}</a>
                                            <a href="{{ path('deleteFile', {'id': file.id}) }}" class="text-red-700 hover:underline" onclick="return confirm('Opravdu chcete soubor {{ file.fileName|upper }} smazat?')">Smazat</a>
                                        </li>
                                    {% endfor %}
                                </ul>
                                </p>
                                <p class="text-m text-gray-600 dark:text-gray-400">
                                    <b class="text-purple-400">Fotografie majetku:</b><br><br>
                                    {% if assets.documentPath %}
                                <div class="relative hidden w-56 h-56 mr-3  md:block">
                                    <img class="object-cover w-full h-full " src="{{ assets.documentPath }}" alt="" loading="lazy">
                                    <div class="absolute inset-0 shadow-inner" aria-hidden="true"></div>
                                </div>
                                {% else %}
                                {% endif %}
                                </p>
                            </div>
                        </div>
                    </main>
                </div>
            </main>
        </div>
    </div>
    <script>
        function openCustomConfirmationModal(assetName) {
            document.getElementById('customConfirmationModal').style.display = 'block';
        }

        function closeCustomConfirmationModal() {
            document.getElementById('customConfirmationModal').style.display = 'none';
        }
    </script>
    <script
            src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"
            integrity="sha512-CNgIRecGo7nphbeZ04Sc13ka07paqdeTu0WR1IM4kNcpmBAUSHSQX0FslNhTDadL4O5SAGapGt4FodqL8My0mA=="
            crossorigin="anonymous"
            referrerpolicy="no-referrer"
    ></script>
    <script>
        let res = document.getElementById('result');

        function GFG_Fun() {
            let img = document.createElement('img');
            img.src =
                'https://media.geeksforgeeks.org/wp-content/uploads/20190529122828/bs21.png';
            document.getElementById('body').appendChild(img);
        }
    </script>
    <script>
        const form = document.getElementById("generate-form");
        const qr = document.getElementById("qrcode");

        // Button submit
        const onGenerateSubmit = (e) => {
            e.preventDefault();

            clearUI();

            const url = document.getElementById("url").value;
            // const size = document.getElementById("size").value;

            // Validate url
            if (url === "") {
                alert("Please enter a URL");
            } else {
                showSpinner();
                // Show spinner for 1 sec
                setTimeout(() => {
                    hideSpinner();
                    generateQRCode(url,);
                    showScanner();
                    // Generate the save button after the qr code image src is ready
                    setTimeout(() => {
                        // Get save url
                        const saveUrl = qr.querySelector("canvas").toDataURL();
                        // Create save button
                        createSaveBtn(saveUrl);
                    }, 50);
                }, 1000);
            }
        };

        // Generate QR code
        const generateQRCode = (url, ) => {
            const qrcode = new QRCode("qrcode", {
                text: url,
                // width: size,
                // height: size,
            });
        };

        // Clear QR code and save button
        const clearUI = () => {
            qr.innerHTML = "";
            const saveBtn = document.getElementById("save-link");
            if (saveBtn) {
                saveBtn.remove();
            }
        };

        // hide  scanner
        const showScanner = () => {
            const scanner = document.getElementById("qrCodeContainer");
            scanner.style.display = "block";
        };

        // Show spinner
        const showSpinner = () => {
            const spinner = document.getElementById("spinner");
            spinner.style.display = "block";
        };

        // Hide spinner
        const hideSpinner = () => {
            const spinner = document.getElementById("spinner");
            spinner.style.display = "none";
        };

        // Create save button to download QR code as image
        const createSaveBtn = (saveUrl) => {
            const link = document.createElement("a");
            link.id = "save-link";
            link.classList =
                'bg-red-500 hover:bg-red-700 text-white font-bold py-2 rounded w-1/3 m-auto my-5';
            link.innerHTML = "Save Image";

            link.href = saveUrl;
            link.download = "qrcode.png";

            document.getElementById("generated").appendChild(link);
        };

        hideSpinner();

        form.addEventListener("submit", onGenerateSubmit);

    </script>
{% endblock %}


